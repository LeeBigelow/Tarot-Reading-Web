# CSS files
`default.css` will be sourced as a css link.

`layout-LAYOUT_NAME.css` files will be sourced by the `tools/make-js-layouts.sh` script to make the `layout-LAYOUT_NAME.js` files for the main `index.html`.

For testing the `test-layout-template.html` can source them as a link.
